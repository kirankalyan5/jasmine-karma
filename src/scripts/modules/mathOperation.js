define(function() {
    return {
        add: function(operand1, operand2) {
            return (operand1 + operand2);
        },

        multiply: function(operand1, operand2) {
            return (operand1 * operand2);
        }
    }
});
